name: onisapp
description: "A new Flutter project."
publish_to: 'none' # Remove esta linha se você deseja publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2
  cupertino_icons: ^1.0.8
  flutter_lints: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # O pacote "flutter_lints" contém lints recomendados para boas práticas.
  # Ele está ativo no arquivo `analysis_options.yaml` localizado na raiz do seu projeto.
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Adiciona a pasta de assets
  assets:
    - assets/
